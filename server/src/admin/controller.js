const pool = require("../../db");
const queries = require("./queries");

const getEmployees = (req, res) => {
    try {
        
    } catch (error) {
        
    }
}

module.exports = {
    getEmployees,
}